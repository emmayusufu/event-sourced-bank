DATABASE_URL=postgres://bank:bank@localhost:5432/bank
PORT=3000

# Set ROLE=follower on a node that should mirror a primary's event log.
# When set, the node also needs PRIMARY_URL and optionally REPLICATION_POLL_MS.
ROLE=primary
PRIMARY_URL=
REPLICATION_POLL_MS=500
